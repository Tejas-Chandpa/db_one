import 'package:db_one/main.dart';
import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class UseDatabase extends StatefulWidget {
  const UseDatabase({Key? key}) : super(key: key);

  @override
  _UseDatabaseState createState() => _UseDatabaseState();
}

class _UseDatabaseState extends State<UseDatabase> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
